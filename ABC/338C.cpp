/*
g++ XXX.cpp
./a.out
*/
#include <bits/stdc++.h>
using namespace std;


int main() {
  int n; cin >> n;
  vector<int>q(n);
  vector<int>a(n);
  vector<int>b(n);
  for (int i=0;i<n;i++) cin >> q.at(i);
  for (int i=0;i<n;i++) cin >> a.at(i);
  for (int i=0;i<n;i++) cin >> b.at(i);
  int an, bn;
  int maxn, index = 0;
  maxn = a.at(0);
  for (int i=0;i<n;i++) if (maxn > a.at(i)){
    maxn = a.at(i);
    index = i;
  }
  an = 
  for (int )
  return 0;
}